import {makeStyles} from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
    logoStyle : {
        flex: 1
    },
    list: {
        width: 250
    },
    wrapperDiv:{
        
    },
    timeButtons: {
        borderRadius:0,
        padding: '4px',
        marginBottom: '10px'
    }
}))

export default useStyles;