import {makeStyles} from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
    logoStyle : {
        flex: 1
    },
    list: {
        width: 250
    },
    wrapperDiv:{
        
    }
}))

export default useStyles;